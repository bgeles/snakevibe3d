{
  "name": "snake-game-3d",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:prod": "vite build --mode production",
    "preview": "vite preview",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "android:sync": "npx cap sync android",
    "android:prepare": "npm run build:prod && npm run android:sync",
    "android:release:aab": "npm run build:prod && npm run android:sync && cd android && ./gradlew bundleRelease",
    "android:release:apk": "npm run build:prod && npm run android:sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor-community/admob": "^7.2.0",
    "@capacitor/android": "^7.1.0",
    "@capacitor/core": "^7.1.0",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.1.0",
    "vite": "^6.2.0"
  }
}
